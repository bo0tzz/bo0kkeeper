{
  "name": "bo0kkeeper-monorepo",
  "version": "0.1.0",
  "description": "Bookkeeping automation for a Dutch zzp",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "pnpm": ">=10.0.0",
    "node": ">=24.0.0"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
